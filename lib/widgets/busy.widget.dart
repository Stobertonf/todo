import 'package:flutter/material.dart';

class TDBusy extends StatelessWidget {
  const TDBusy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
